/// <reference types="react" />
import { type User } from "./useUser";
import { ConversationFetchOptions } from "./services/conversations";
export type ChatbotProps = {
    children: React.ReactElement | React.ReactElement[];
    darkMode?: boolean;
    isExperimental?: boolean;
    maxInputCharacters?: number;
    maxCommentCharacters?: number;
    name?: string;
    serverBaseUrl?: string;
    shouldStream?: boolean;
    tck?: string;
    user?: User;
    fetchOptions?: ConversationFetchOptions;
};
export declare function Chatbot({ children, serverBaseUrl, shouldStream, user, name, fetchOptions, ...props }: ChatbotProps): import("react/jsx-runtime").JSX.Element;
