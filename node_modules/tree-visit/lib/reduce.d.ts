import { IndexPath } from './indexPath';
import { BaseOptions } from './options';
export declare type ReduceOptions<T, R> = BaseOptions<T> & {
    /**
     * The initial result value.
     */
    initialResult: R;
    /**
     * Return the next result value.
     */
    nextResult: (result: R, node: T, indexPath: IndexPath) => R;
};
export declare function reduce<T, R>(node: T, options: ReduceOptions<T, R>): R;
