import { IndexPath } from './indexPath';
import { BaseOptions } from './options';
export declare type DiagramType = 'directory' | 'box';
export declare type DiagramOptions<T> = BaseOptions<T> & {
    getLabel: (node: T, indexPath: IndexPath) => string;
    type?: DiagramType;
    flattenSingleChildNodes?: boolean;
};
/**
 * Generate a diagram of the tree, as a string.
 */
export declare function diagram<T>(node: T, options: DiagramOptions<T>): string;
