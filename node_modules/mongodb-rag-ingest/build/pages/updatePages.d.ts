import { Page, PageStore } from "mongodb-rag-core";
import { DataSource } from "../sources/DataSource";
/**
  Fetches pages from data sources and stores those that have changed in the data
  store.
 */
export declare const updatePages: ({ sources, pageStore, }: {
    sources: DataSource[];
    pageStore: PageStore;
}) => Promise<void>;
/**
  Persists pages that have changed.
 */
export declare const persistPages: ({ store, pages, sourceName, }: {
    store: PageStore;
    pages: Page[];
    sourceName: string;
}) => Promise<void>;
//# sourceMappingURL=updatePages.d.ts.map