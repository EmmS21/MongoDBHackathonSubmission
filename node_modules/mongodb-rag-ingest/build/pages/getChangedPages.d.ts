import { Page, PersistedPage } from "mongodb-rag-core";
/**
  Given sets of old and new pages, returns the pages that need to be created,
  updated, or deleted.
 */
export declare const getChangedPages: ({ oldPages: oldPagesIn, newPages: newPagesIn, }: {
    oldPages: Omit<PersistedPage, "updated">[];
    newPages: Page[];
}) => Promise<{
    deleted: PersistedPage[];
    created: PersistedPage[];
    updated: PersistedPage[];
}>;
//# sourceMappingURL=getChangedPages.d.ts.map