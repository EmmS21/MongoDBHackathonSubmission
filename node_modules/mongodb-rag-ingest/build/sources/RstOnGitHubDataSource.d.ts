import { Document } from "langchain/document";
import { MakeGitHubDataSourceArgs } from "./GitHubDataSource";
/**
  Loads an rST docs site from a GitHub repo.
 */
export declare const makeRstOnGitHubDataSource: <MetadataType extends Record<string, unknown>>({ name, repoUrl, repoLoaderOptions, pathToPageUrl, getMetadata, }: Omit<MakeGitHubDataSourceArgs, "handleDocumentInRepo"> & {
    /**
      Transform a filepath in the repo to a full URL for the corresponding Page object.
     */
    pathToPageUrl(pathInRepo: string): string;
    /**
      Return arbitrary metadata for the given document.
     */
    getMetadata?(info: {
        document: Document<{
            source: string;
        }>;
        url: string;
        bodyMarkdown: string;
        title?: string;
    }): MetadataType | undefined;
}) => import("./DataSource").DataSource;
//# sourceMappingURL=RstOnGitHubDataSource.d.ts.map