import { Embedder, EmbeddedContentStore, PersistedPage, PageStore } from "mongodb-rag-core";
import { ChunkOptions } from "./chunkPage";
/**
  (Re-)embeddedContent the pages in the page store that have changed since the given date
  and stores the embeddedContent in the embeddedContent store.
 */
export declare const updateEmbeddedContent: ({ since, embeddedContentStore, pageStore, sourceNames, embedder, chunkOptions, }: {
    since: Date;
    embeddedContentStore: EmbeddedContentStore;
    pageStore: PageStore;
    embedder: Embedder;
    chunkOptions?: Partial<ChunkOptions> | undefined;
    sourceNames?: string[] | undefined;
}) => Promise<void>;
export declare const updateEmbeddedContentForPage: ({ page, store, embedder, chunkOptions, }: {
    page: PersistedPage;
    store: EmbeddedContentStore;
    embedder: Embedder;
    chunkOptions?: Partial<ChunkOptions> | undefined;
}) => Promise<void>;
//# sourceMappingURL=updateEmbeddedContent.d.ts.map